/* 
1.1 variable (let const)
1.2 condition(<,>, ===, !==, <=, >=) && ||, if else
1.3 array: declare, length, index, push, pop, indexOf, includes
1.4 for loop, While
1.5 function return parameter
1.6 object property, including array, object


===================
ES6
1.template string
2.spread (...)
2.1copy an array then add a new element array
2.2 use filter to remove an element from an array;
3. arrow function
3.1 single parameter
3.2 no parameter
3.3 multiple parameter;
4. Destructuring: object Destructuring array Destructuring
5 object declaration shorthand
6. function default parameter default value;
7. optional chaining ?
-----------------------------
Browser api
1.local storage session storage
2.location api
3. history api
4.fetch

-----------------------
others
1. array: map, foreach, filter, find
2. ternary operator
3. logical and logical or && ||;
4. JSON(stringify, parse)
5. +, ''

*/

const x = 50;
const y = 90;

const obj = {
    x:x,
    y:y
}

//shortcut
const obj1 ={x, y}
const name = 'solimollah'
const area = 'dhaka'

const nobab = {
    name: name,
    area: area
}

const nobab3 = {name, area}